class Solution{
    int DivisibleByEight(String s){
        if(s.length()<=3)return Integer.parseInt(s)%8 ==0 ? 1: -1;
        return Integer.parseInt(s.substring(s.length()-4))%8==0 ? 1 :-1;
    }
}
