class Solution
{
    public int peakElement(int[] arr,int n)
    {
       int max=arr[0],st=0;
       for(int i=0;i<n;i++){
           if(max<arr[i]){
                max=arr[i];
                   st=i;
               
              
           }
       }
     
     return st;
    }
}
